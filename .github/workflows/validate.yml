name: Validate

on:
  push:
  pull_request:

jobs:
  native:
    name: Native checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: jdx/mise-action@v4

      - run: mise run ci
